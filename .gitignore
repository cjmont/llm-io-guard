# Rust
/target
**/*.rs.bk

# Node
node_modules/
packages/*/dist/

# build outputs (regenerated in CI / by `npm run build`)
# binding.js + binding.d.ts ARE committed (needed by the publish job's TS build)
packages/*/*.node
packages/*/npm/*/*.node
packages/*/wasm/

# misc
*.log
.DS_Store
artifacts/

# Claude Code local config
.claude/
